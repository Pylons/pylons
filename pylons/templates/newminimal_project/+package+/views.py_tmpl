from pylons.views import expose

class MyController(object):
    def __init__(self, request):
        self.request = request

    @expose(renderer='mytemplate.mak')
    def index(self):
        return {'project':'{{project}}'}
