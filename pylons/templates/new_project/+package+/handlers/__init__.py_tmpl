"""Handlers package"""
def add_handlers(config):
    config.add_handler('action', '/{action}', '.hello:HelloHandler')
    config.add_handler('home', '/', '.hello:HelloHandler',
                       action='index')
