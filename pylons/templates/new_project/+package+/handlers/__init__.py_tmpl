"""Handlers package"""
def add_handlers(config):
    config.add_handler('action', '/{action}', '{{package}}.handlers.hello:HelloHandler')
    config.add_handler('home', '/', '{{package}}.handlers.hello:HelloHandler',
                       action='index')
